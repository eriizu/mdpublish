---
order: 2
---

# Publishing

The generated directory can be uploaded to any static host.

## Subpath hosting

Use `--base /notes/` when the site is served below a domain subpath.

## Direct links

Every note is emitted as a directory containing `index.html`, so URLs remain
pleasant and work when refreshed.

Return to [[Home]].
